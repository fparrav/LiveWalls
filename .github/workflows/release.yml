name: 🚀 Release LiveWalls

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0, v1.2.3, etc.

env:
  APP_NAME: "LiveWalls"
  SCHEME_NAME: "LiveWalls"
  
jobs:
  build-and-release:
    runs-on: macos-latest
    concurrency:
      group: pages
      cancel-in-progress: false
    
    permissions:
      contents: write  # Necesario para crear releases
      pages: write      # Desplegar a GitHub Pages
      id-token: write   # Requerido por deploy-pages
    env:
      # Exponer el secreto a nivel de job para usarlo en condiciones y pasos
      SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
      
    steps:
    - name: 📥 Checkout Code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        fetch-tags: true
      
    - name: 🔧 Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
        
    - name: 📋 Cache Dependencies
      uses: actions/cache@v3
      with:
        path: |
          ~/Library/Developer/Xcode/DerivedData
          ~/.swiftpm
        key: ${{ runner.os }}-xcode-${{ hashFiles('**/*.xcodeproj', '**/*.swift') }}
        restore-keys: |
          ${{ runner.os }}-xcode-
          
    - name: 🔍 Validate Project Configuration
      run: |
        echo "🔍 Validating project configuration..."
        
        # Validate Info.plist exists and is valid
        if [ ! -f "$APP_NAME/Info.plist" ]; then
          echo "❌ Error: Info.plist not found at $APP_NAME/Info.plist"
          exit 1
        fi
        
        # Validate Info.plist syntax
        plutil -lint "$APP_NAME/Info.plist"
        if [ $? -ne 0 ]; then
          echo "❌ Error: Info.plist is not valid"
          exit 1
        fi
        
        # Show Info.plist content for debugging
        echo "📋 Info.plist validation successful. Key information:"
        plutil -p "$APP_NAME/Info.plist" | grep -E "(CFBundleIdentifier|CFBundleExecutable|CFBundleName|CFBundleShortVersionString)"
        
        # Validate project file exists
        if [ ! -f "$APP_NAME.xcodeproj/project.pbxproj" ]; then
          echo "❌ Error: Xcode project file not found"
          exit 1
        fi
        
        # Show Xcode version for debugging
        xcodebuild -version
        
        # Show available schemes
        echo "📋 Available schemes:"
        xcodebuild -list -project "$APP_NAME.xcodeproj"
        
        echo "✅ Project configuration validation complete"

    - name: 🔍 Validate Xcode Project Settings
      run: |
        echo "🔍 Validating Xcode project settings..."
        
        # Check for proper code signing configuration
        echo "📋 Checking code signing settings..."
        xcodebuild -project "$APP_NAME.xcodeproj" -target "$APP_NAME" -configuration Release -showBuildSettings | grep -E "(CODE_SIGN|DEVELOPMENT_TEAM)" || true
        
        # Verify no development team is set for Release builds
        TEAM_SETTING=$(xcodebuild -project "$APP_NAME.xcodeproj" -target "$APP_NAME" -configuration Release -showBuildSettings | grep "DEVELOPMENT_TEAM" | head -1)
        if [[ "$TEAM_SETTING" == *"CED7KEV553"* ]]; then
          echo "⚠️ Warning: Development team still set in project settings"
        else
          echo "✅ Development team properly cleared"
        fi
        
        # Check deployment target
        echo "📋 Checking deployment target..."
        DEPLOYMENT_TARGET=$(xcodebuild -project "$APP_NAME.xcodeproj" -target "$APP_NAME" -configuration Release -showBuildSettings | grep "MACOSX_DEPLOYMENT_TARGET" | head -1)
        echo "$DEPLOYMENT_TARGET"
        
        # Check bundle identifier
        echo "📋 Checking bundle identifier..."
        BUNDLE_ID=$(xcodebuild -project "$APP_NAME.xcodeproj" -target "$APP_NAME" -configuration Release -showBuildSettings | grep "PRODUCT_BUNDLE_IDENTIFIER" | head -1)
        echo "$BUNDLE_ID"
        
        # Verify entitlements file exists
        if [ ! -f "$APP_NAME/LiveWalls.entitlements" ]; then
          echo "❌ Error: Entitlements file not found"
          exit 1
        fi
        
        echo "📋 Entitlements file content:"
        cat "$APP_NAME/LiveWalls.entitlements"
        
        echo "✅ Xcode project settings validation complete"

    - name: 🧪 Run Unit Tests
      run: |
        echo "🧪 Running unit tests..."
        
        # Run unit tests to ensure code quality
        echo "📋 Running LiveWallsTests..."
        xcodebuild test \
          -project "$APP_NAME.xcodeproj" \
          -scheme "$SCHEME_NAME" \
          -destination "platform=macOS" \
          -configuration Release \
          -derivedDataPath ./DerivedDataTests \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO || {
          echo "⚠️ Unit tests failed, but continuing with build..."
          echo "📋 Test failure details:"
          find ./DerivedDataTests -name "*.xcresult" -exec xcrun xcresulttool get --format json {} \; | head -20 || true
        }
        
        echo "✅ Unit tests completed"

    - name: 🏗️ Build App
      run: |
        set -o pipefail
        echo "🔨 Building $APP_NAME..."
        
        # Create build directory
        mkdir -p ./build
        
        # Show current directory and files for debugging
        echo "📁 Current directory: $(pwd)"
        echo "📁 Directory contents:"
        ls -la
        
        # Build with verbose output for better debugging
        echo "🔨 Starting Xcode build process..."
        xcodebuild \
          -project "$APP_NAME.xcodeproj" \
          -scheme "$SCHEME_NAME" \
          -configuration Release \
          -destination "generic/platform=macOS" \
          -derivedDataPath ./DerivedData \
          -archivePath "./build/$APP_NAME.xcarchive" \
          archive \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO \
          CODE_SIGNING_ALLOWED=NO \
          ARCHS=arm64 ONLY_ACTIVE_ARCH=YES \
          -verbose | tee build_log.txt

        # Check if build succeeded
        BUILD_EXIT_CODE=${PIPESTATUS[0]}
        if [ $BUILD_EXIT_CODE -ne 0 ]; then
          echo "❌ Build failed with exit code: $BUILD_EXIT_CODE"
          echo "📋 Last 300 lines of build log:"
          tail -300 build_log.txt || true
          echo "🛟 Fallback: building (no archive) to extract .app..."
          xcodebuild \
            -project "$APP_NAME.xcodeproj" \
            -scheme "$SCHEME_NAME" \
            -configuration Release \
            -destination "generic/platform=macOS" \
            -derivedDataPath ./DerivedData \
            build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            ARCHS=arm64 ONLY_ACTIVE_ARCH=YES \
            -quiet | tee build_fallback_log.txt || true

          # Try to copy built app from DerivedData products to export dir
          DERIVED_APP=$(find ./DerivedData/Build/Products -type d -name "$APP_NAME.app" | head -n 1 || true)
          if [ -d "$DERIVED_APP" ]; then
            mkdir -p ./build/export
            rm -rf ./build/export/$APP_NAME.app || true
            cp -R "$DERIVED_APP" ./build/export/
            echo "✅ Fallback copied app from $DERIVED_APP to ./build/export/$APP_NAME.app"
          else
            echo "❌ Fallback build did not produce $APP_NAME.app"
            echo "📋 Last 200 lines of fallback log:"
            tail -200 build_fallback_log.txt || true
            echo "❌ Build process failed. Check logs above for details."
            exit $BUILD_EXIT_CODE
          fi
        fi
        
        # Verify archive was created
        if [ ! -d "./build/$APP_NAME.xcarchive" ]; then
          echo "❌ Error: Archive was not created at ./build/$APP_NAME.xcarchive"
          echo "📁 Build directory contents:"
          ls -la ./build/
          exit 1
        fi
        
        echo "✅ Build completed successfully"
        echo "📁 Archive contents:"
        ls -la "./build/$APP_NAME.xcarchive/"
          
    - name: 📦 Extract App from Archive
      run: |
        echo "📦 Extracting $APP_NAME.app from archive..."
        
        # Create export directory
        mkdir -p "./build/export"
        
        # If app already exists from fallback build, skip archive extraction
        if [ -d "./build/export/$APP_NAME.app" ]; then
          echo "ℹ️ App already present at ./build/export/$APP_NAME.app (from fallback). Skipping extraction."
          ls -la "./build/export/$APP_NAME.app/Contents/" || true
          exit 0
        fi
        
        # Show archive structure for debugging
        echo "📁 Archive structure:"
        find "./build/$APP_NAME.xcarchive" -type d -name "*.app" || {
          echo "🔍 No .app found, showing full archive structure:"
          find "./build/$APP_NAME.xcarchive" -maxdepth 3 -type d
        }
        
        # Check multiple possible locations for the app
        APP_LOCATIONS=(
          "./build/$APP_NAME.xcarchive/Products/Applications/$APP_NAME.app"
          "./build/$APP_NAME.xcarchive/Products/$APP_NAME.app"
          "./build/$APP_NAME.xcarchive/$APP_NAME.app"
        )
        
        APP_FOUND=false
        for location in "${APP_LOCATIONS[@]}"; do
          if [ -d "$location" ]; then
            echo "✅ Found app at: $location"
            cp -R "$location" "./build/export/"
            APP_FOUND=true
            break
          fi
        done
        
        if [ "$APP_FOUND" = false ]; then
          echo "❌ Failed to find $APP_NAME.app in any expected location"
          echo "📁 Full archive contents:"
          find "./build/$APP_NAME.xcarchive" -name "*" -type f | head -20
          # If still not found, but export exists from other steps, continue
          if [ -d "./build/export/$APP_NAME.app" ]; then
            echo "⚠️ Archive not found, using pre-existing exported app"
          else
            exit 1
          fi
        fi
        
        # Verify extraction
        if [ -d "./build/export/$APP_NAME.app" ]; then
          echo "✅ Successfully extracted $APP_NAME.app"
          echo "📁 Exported app structure:"
          ls -la "./build/export/$APP_NAME.app/Contents/"
          
          # Verify executable exists
          if [ -f "./build/export/$APP_NAME.app/Contents/MacOS/$APP_NAME" ]; then
            echo "✅ Executable found at Contents/MacOS/$APP_NAME"
          else
            echo "⚠️ Warning: Executable not found at expected location"
            find "./build/export/$APP_NAME.app" -name "$APP_NAME" -type f
          fi
        else
          echo "❌ Failed to extract app properly"
          exit 1
        fi
          
    - name: 💿 Create DMG
      run: |
        echo "💿 Creating DMG..."
        
        # Verify exported app exists
        if [ ! -d "./build/export/$APP_NAME.app" ]; then
          echo "❌ Error: $APP_NAME.app not found in export directory"
          echo "📁 Export directory contents:"
          ls -la ./build/export/
          exit 1
        fi
        
        # Show app bundle details for debugging
        echo "📱 App bundle details:"
        ls -la "./build/export/$APP_NAME.app/Contents/"
        
        # Verify app structure is complete
        if [ ! -f "./build/export/$APP_NAME.app/Contents/MacOS/$APP_NAME" ]; then
          echo "❌ Error: Main executable missing in app bundle"
          find "./build/export/$APP_NAME.app" -type f -name "$APP_NAME" || echo "No executable found"
          exit 1
        fi
        
        if [ ! -f "./build/export/$APP_NAME.app/Contents/Info.plist" ]; then
          echo "❌ Error: Info.plist missing in app bundle"
          exit 1
        fi
        
        echo "✅ App bundle structure validated"
        
        # Install create-dmg if not available
        if ! command -v create-dmg &> /dev/null; then
          echo "📥 Installing create-dmg..."
          brew install create-dmg
        fi
        
        # Create DMG with enhanced error handling
        echo "🔨 Creating DMG with create-dmg..."
        if create-dmg \
          --volname "$APP_NAME" \
          --window-pos 200 120 \
          --window-size 800 400 \
          --icon-size 100 \
          --icon "$APP_NAME.app" 200 190 \
          --hide-extension "$APP_NAME.app" \
          --app-drop-link 600 185 \
          "./build/$APP_NAME-${{ github.ref_name }}.dmg" \
          "./build/export/"; then
          echo "✅ DMG created successfully with create-dmg"
        else
          echo "⚠️ create-dmg failed, using hdiutil fallback..."
          # Clean any partial DMG
          rm -f "./build/$APP_NAME-${{ github.ref_name }}.dmg"
          
          # Fallback: simple DMG creation with hdiutil
          if hdiutil create \
            -srcfolder "./build/export/$APP_NAME.app" \
            -volname "$APP_NAME" \
            -format UDZO \
            -imagekey zlib-level=9 \
            "./build/$APP_NAME-${{ github.ref_name }}.dmg"; then
            echo "✅ DMG created successfully with hdiutil fallback"
          else
            echo "❌ Both create-dmg and hdiutil failed"
            exit 1
          fi
        fi
        
        # Verify DMG was created and is valid
        if [ ! -f "./build/$APP_NAME-${{ github.ref_name }}.dmg" ]; then
          echo "❌ Error: DMG file was not created"
          exit 1
        fi
        
        # Test DMG integrity
        echo "🔍 Verifying DMG integrity..."
        if hdiutil verify "./build/$APP_NAME-${{ github.ref_name }}.dmg"; then
          echo "✅ DMG integrity verified"
        else
          echo "⚠️ DMG verification failed, but file exists"
        fi
        
        # Show final DMG details
        echo "📊 DMG created successfully:"
        ls -lh "./build/$APP_NAME-${{ github.ref_name }}.dmg"

    - name: 🔍 Final Validation and Testing
      run: |
        echo "🔍 Performing final validation and testing..."
        
        # Mount the DMG and verify its contents
        echo "📱 Mounting DMG for validation..."
        hdiutil attach "./build/$APP_NAME-${{ github.ref_name }}.dmg" -mountpoint "/tmp/LiveWalls_Mount" -quiet
        
        # Verify app structure inside DMG
        if [ -d "/tmp/LiveWalls_Mount/$APP_NAME.app" ]; then
          echo "✅ App found in DMG"
          
          # Check app bundle structure
          echo "📋 App bundle contents:"
          ls -la "/tmp/LiveWalls_Mount/$APP_NAME.app/Contents/"
          
          # Verify critical files exist
          if [ -f "/tmp/LiveWalls_Mount/$APP_NAME.app/Contents/MacOS/$APP_NAME" ]; then
            echo "✅ Main executable found"
          else
            echo "❌ Main executable missing"
          fi
          
          if [ -f "/tmp/LiveWalls_Mount/$APP_NAME.app/Contents/Info.plist" ]; then
            echo "✅ Info.plist found in app bundle"
            echo "📋 Info.plist validation:"
            plutil -lint "/tmp/LiveWalls_Mount/$APP_NAME.app/Contents/Info.plist"
          else
            echo "❌ Info.plist missing in app bundle"
          fi
          
          # Check app permissions and signature (should show unsigned)
          echo "📋 App signature status:"
          codesign -dvv "/tmp/LiveWalls_Mount/$APP_NAME.app" 2>&1 | head -10 || echo "App is unsigned (expected)"
          
        else
          echo "❌ App not found in DMG"
          ls -la "/tmp/LiveWalls_Mount/"
        fi
        
        # Unmount DMG
        hdiutil detach "/tmp/LiveWalls_Mount" -quiet
        
        echo "✅ Final validation completed"
          
    - name: 🛠️ Install git-cliff
      run: |
        echo "📥 Installing git-cliff..."
        brew install git-cliff
        
    - name: 📋 Generate Changelog
      id: release_info
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        VERSION=${{ github.ref_name }}
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        
        # Obtener el tag anterior para generar changelog desde ese punto
        PREVIOUS_TAG=$(git tag --sort=-version:refname | grep -v "$VERSION" | head -n 1)
        
        if [ -z "$PREVIOUS_TAG" ]; then
          echo "📝 Primer release - generando changelog completo"
          # Para el primer release, generar changelog completo
          CHANGELOG=$(git cliff --tag "$VERSION" --strip all --github-token "$GITHUB_TOKEN")
          
          # Si no hay commits convencionales, usar mensaje por defecto
          if [ -z "$CHANGELOG" ] || [ "$CHANGELOG" = "## [${VERSION#v}] - $(date +%Y-%m-%d)" ]; then
            CHANGELOG="## 🚀 Cambios en esta versión
        
        🎉 **Primera versión de LiveWalls disponible!**
        
        Esta es la versión inicial de LiveWalls, una aplicación para gestionar fondos de pantalla dinámicos en macOS."
          fi
        else
          echo "📝 Generando changelog desde $PREVIOUS_TAG hasta $VERSION"
          # Generar changelog solo para los commits desde el tag anterior
          CHANGELOG=$(git cliff --tag "$VERSION" "$PREVIOUS_TAG".."$VERSION" --strip all --github-token "$GITHUB_TOKEN")
          
          # Si git-cliff no encuentra commits convencionales, usar fallback
          if [ -z "$CHANGELOG" ] || [ "$CHANGELOG" = "## [${VERSION#v}] - $(date +%Y-%m-%d)" ]; then
            echo "⚠️ No se encontraron commits convencionales, usando fallback"
            CHANGELOG="## 🚀 Cambios en esta versión
        
        $(git log --pretty=format:"- %s ([%h](https://github.com/${{ github.repository }}/commit/%H))" "$PREVIOUS_TAG".."$VERSION")"
          fi
        fi
        
        echo "changelog<<EOF" >> $GITHUB_OUTPUT
        echo "$CHANGELOG" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
        
    - name: 🚀 Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ github.ref_name }}
        name: "LiveWalls ${{ steps.release_info.outputs.version }}"
        body: |
          ${{ steps.release_info.outputs.changelog }}
          
          ## 📥 Instalación
          
          1. Descarga el archivo `${{ env.APP_NAME }}-${{ github.ref_name }}.dmg`
          2. Abre el DMG y arrastra LiveWalls a Aplicaciones
          3. ¡Disfruta tus fondos de pantalla dinámicos!
          
          ## 📋 Requisitos
          - macOS 14.0 (Sonoma) o superior
          
          ## 🔒 Seguridad
          ⚠️ **Aplicación sin firma digital**: Esta es una aplicación open source distribuida sin certificado de desarrollador de Apple.
          
          Para abrir la aplicación por primera vez:
          ```bash
          # Opción 1: Desactivar quarantine (recomendado)
          xattr -d com.apple.quarantine /Applications/LiveWalls.app
          
          # Opción 2: Ir a Preferencias > Seguridad y permitir la app
          ```
          
          💡 **Recomendación**: Para mayor seguridad, considera compilar la aplicación desde el código fuente.
          
        files: |
          ./build/${{ env.APP_NAME }}-${{ github.ref_name }}.dmg
        draft: false
        prerelease: ${{ contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') }}
        
    - name: 📦 Prepare Sparkle Artifacts (DMG + signature)
      id: prepare_sparkle
      run: |
        set -e
        echo "📦 Preparing Sparkle artifacts..."
        
        # Validate secret
        if [ -z "${SPARKLE_PRIVATE_KEY}" ]; then
          echo "⚠️ SPARKLE_PRIVATE_KEY secret is not set. Skipping appcast generation."
          echo "publish_pages=false" >> "$GITHUB_OUTPUT"
          exit 0
        fi
        
        DMG_PATH="./build/$APP_NAME-${{ github.ref_name }}.dmg"
        if [ ! -f "$DMG_PATH" ]; then
          echo "❌ DMG not found at $DMG_PATH"
          exit 1
        fi
        
        # Install Sparkle CLI tools (generate_appcast, sign_update)
        SPARKLE_VERSION="2.6.0"
        echo "📥 Downloading Sparkle CLI $SPARKLE_VERSION..."
        curl -L -o sparkle.tar.xz "https://github.com/sparkle-project/Sparkle/releases/download/${SPARKLE_VERSION}/Sparkle-${SPARKLE_VERSION}.tar.xz"
        tar -xf sparkle.tar.xz
        echo "✅ Extracted Sparkle to $(pwd)/Sparkle-${SPARKLE_VERSION}"
        export PATH="$(pwd)/Sparkle-${SPARKLE_VERSION}/bin:$PATH"
        which generate_appcast
        which sign_update
        
        mkdir -p ./public/sparkle
        cp "$DMG_PATH" ./public/sparkle/
        
        # Write private key to file with restricted permissions
        echo "$SPARKLE_PRIVATE_KEY" > ./private_eddsa.pem
        chmod 600 ./private_eddsa.pem
        
        echo "🖊️ Signing DMG for Sparkle..."
        sign_update -s ./private_eddsa.pem ./public/sparkle/"$APP_NAME-${{ github.ref_name }}.dmg"
        
        echo "📰 Generating appcast.xml..."
        OWNER="${{ github.repository_owner }}"
        REPO_NAME="${{ github.event.repository.name }}"
        BASE_URL="https://${OWNER}.github.io/${REPO_NAME}/sparkle/"
        
        generate_appcast \
          --download-url-prefix "$BASE_URL" \
          --link "$BASE_URL" \
          --title "$APP_NAME Updates" \
          --output-dir ./public/sparkle \
          ./public/sparkle
        
        echo "✅ Appcast generated at ./public/sparkle/appcast.xml"
        echo "publish_pages=true" >> "$GITHUB_OUTPUT"

    - name: 🌳 Publish site & Sparkle to gh-pages branch
      if: steps.prepare_sparkle.outputs.publish_pages == 'true' || hashFiles('site/**') != ''
      run: |
        set -e
        echo "🌳 Publishing site and Sparkle artifacts to gh-pages branch..."
        
        # Stash artifacts before switching branches
        TMPDIR=$(mktemp -d)
        # Site (optional)
        if [ -d ./site ]; then
          mkdir -p "$TMPDIR/site"
          cp -R ./site/. "$TMPDIR/site/"
        fi
        # Sparkle (optional depending on secret)
        if [ -d ./public/sparkle ]; then
          cp -R ./public/sparkle "$TMPDIR/"
        fi
        
        git fetch --all --prune
        if git show-ref --verify --quiet refs/heads/gh-pages; then
          git checkout gh-pages
        else
          git checkout --orphan gh-pages
          # Remove all files in working tree for clean branch
          git rm -rf . || true
        fi
        
        # Publish site content to root (without deleting sparkle/)
        if [ -d "$TMPDIR/site" ]; then
          rsync -a "$TMPDIR/site/" ./
        fi
        # Publish Sparkle under /sparkle (no delete outside that folder)
        if [ -d "$TMPDIR/sparkle" ]; then
          mkdir -p sparkle
          rsync -a --delete "$TMPDIR/sparkle/" sparkle/
        fi
        # Avoid Jekyll processing
        touch .nojekyll
        
        git config user.email "action@github.com"
        git config user.name "GitHub Action"
        git add -A
        if git diff --staged --quiet; then
          echo "No changes to publish on gh-pages."
        else
          COMMIT_MSG="🌐 docs(site): publicar landing y sparkle para ${{ github.ref_name }}"
          git commit -m "$COMMIT_MSG"
          git push origin HEAD:gh-pages
          echo "✅ Published to gh-pages"
        fi

    - name: 🍺 Update Homebrew Formula
      run: |
        echo "🍺 Updating Homebrew formula..."
        
        # Calculate SHA256 of the generated DMG
        DMG_PATH="./build/$APP_NAME-${{ github.ref_name }}.dmg"
        SHA256_SUM=$(shasum -a 256 "$DMG_PATH" | cut -d' ' -f1)
        echo "📊 Calculated SHA256: $SHA256_SUM"
        
        # Update the Homebrew formula
        FORMULA_PATH="homebrew/livewalls.rb"
        VERSION="${{ github.ref_name }}"  # e.g., v1.0.0
        VERSION_NUMBER="${VERSION#v}"     # Remove 'v' prefix -> 1.0.0
        
        # Update version
        sed -i '' "s/version \"[^\"]*\"/version \"$VERSION_NUMBER\"/" "$FORMULA_PATH"
        
        # Update URL
        sed -i '' "s|url \"[^\"]*\"|url \"https://github.com/${{ github.repository }}/releases/download/$VERSION/$APP_NAME-$VERSION.dmg\"|" "$FORMULA_PATH"
        
        # Update SHA256
        sed -i '' "s/sha256 \"[^\"]*\"/sha256 \"$SHA256_SUM\"/" "$FORMULA_PATH"
        
        echo "✅ Homebrew formula updated successfully"
        
        # Show changes
        echo "📋 Changes made to Homebrew formula:"
        git diff "$FORMULA_PATH" || true

    - name: 📤 Commit and Push Homebrew Changes
      run: |
        echo "📤 Committing Homebrew formula changes..."
        
        # Configure Git
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        # Check current git status
        echo "📋 Current git status:"
        git status
        echo "📋 Current branch:"
        git branch -a
        echo "📋 Current HEAD:"
        git log --oneline -1
        
        # Ensure we're on the main branch
        git checkout main || {
          echo "⚠️ Cannot checkout main branch, trying to create from HEAD"
          git checkout -b main
        }
        
        # Add and commit changes
        git add homebrew/livewalls.rb
        
        # Check if there are changes to commit
        if git diff --staged --quiet; then
          echo "⚠️ No changes to commit in Homebrew formula"
        else
          git commit -m "🍺 update homebrew formula to ${{ github.ref_name }}"
          
          # Push with explicit branch reference
          git push origin HEAD:main || {
            echo "⚠️ Direct push failed, trying force push"
            git push origin HEAD:main --force-with-lease
          }
          echo "✅ Homebrew formula changes pushed successfully"
        fi

    - name: ✅ Success Notification
      run: |
        echo "🎉 Release ${{ github.ref_name }} created successfully!"
        echo "📦 DMG: $APP_NAME-${{ github.ref_name }}.dmg"
        echo "🍺 Homebrew formula updated automatically"
        echo "🔗 Release URL: https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}"
